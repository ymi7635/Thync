# ============================================
# 根目錄 .gitignore — 管「不管在哪一層都不該進版控」的東西
# client/、server/ 底下各自的 .gitignore 仍然保留、繼續管各自資料夾的規則
# ============================================

# 作業系統自動產生的垃圾檔案（跟專案內容無關，換一台電腦打開資料夾就會冒出來）
.DS_Store          # macOS Finder 的資料夾中繼資料
Thumbs.db          # Windows 縮圖快取

# 編輯器/IDE 個人設定（每個人習慣不同，不該逼別人共用你的設定）
.vscode/
.idea/

# 資料庫備份/匯出檔（跟 db/ 資料夾裡「版控管理的 schema 檔案」分開處理；
# 這類檔案通常是手動備份整個資料庫時產生的，可能包含當下的真實/測試資料，
# 體積也可能很大，不適合進 Git）
*.sql.bak
*.dump

# 環境變數（以防有人不小心在根目錄建立，client/、server/ 各自的 .env 已經各自處理過）
.env
.env.local

# 暫存測試用的雜項檔案
*.tmp
*.bak
