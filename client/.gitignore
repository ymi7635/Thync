# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# ---- 相依套件 ----
# node_modules 用 npm install 就能重建，體積龐大（幾百 MB 起跳），絕對不該進 Git
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ---- 測試 ----
/coverage

# ---- Next.js 建置產物 ----
# .next/ 是 `next dev` / `next build` 自動生成的快取與編譯結果，每次啟動都會重新產生
/.next/
/out/

# ---- 正式打包產物 ----
/build

# ---- 作業系統雜項 ----
.DS_Store
*.pem

# ---- 除錯/log 檔 ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ---- Vercel 部署快取 ----
.vercel

# ---- TypeScript（雖然本專案主要用 .js，但保留以防萬一）----
*.tsbuildinfo
next-env.d.ts

# ---- 使用者上傳的圖片 ----
# 2026-08-12：為了讓履歷展示網站在 Vercel/Render 上也看得到圖片（不接雲端圖床），
# 資料庫已縮減成只保留「有圖片」的 50 個商品／10 位會員／10 篇文章，
# 硬碟上也只留下對應這批資料的圖片，全部改成「放行」讓 git 追蹤、一起推上 GitHub。
#
# 注意：之後如果在本機後台管理系統上傳新的商品圖／文章圖／大頭貼，
# 這些新檔案會落在同一批資料夾裡，`git add .` 時會被一併加入版控——
# 如果只是本機測試用、不想真的推上 GitHub，記得手動排除該筆檔案，
# 或是額外用 `git status` 確認清單後再 commit。
